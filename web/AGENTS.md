# Working on the web app

Rules for `web/`, on top of the repo-wide rules in the root [`AGENTS.md`](../AGENTS.md). Read both before changing anything here.

`web/` is the React SPA: Vite 7, React 19, TanStack Router file routes, plain JavaScript with JSDoc. Using the app needs a valid session. Before changing any UI, read [`docs/design.md`](../docs/design.md).

## Commands

```bash
npm run dev        # Vite at http://localhost:5173, no service worker
npm run build      # production build, to check it compiles; Caddy's image builds its own
```

`npm run dev` proxies `/api` to the Docker stack at `https://ludi.local:3000`, so signing in needs `docker compose up` ([README](../README.md)). The stack hot-reloads the mounted `web/` folder, so `ludi.local` and a phone both show a saved change without a rebuild.

**The service worker exists only in production builds.** In development Caddy answers `/sw.js` with a script that removes one left in the browser. To check the worker itself: `COMPOSE_FILE=docker-compose.yml docker compose up -d --build`.

**There are no automated tests yet.** Check changes in a browser at 390 px, signed in with a demo account, including offline, night mode, and reduced motion.

## Layout

```
index.html            the shell; no inline scripts
vite.config.js        router plugin, React, and the PWA manifest and service worker
public/               the app icon and the favicon, and the PNGs rendered from them
src/
  app/                the shell: main.jsx (router, service worker, first theme), the root layout
                      and its guard, ThemeProvider, the menu, updates
  routes/             one file per screen; the file name is the URL
  routeTree.gen.js    generated by the router plugin; never edit it, but commit it
  features/           one folder per module: account, family, voice, toys, materials, home,
                      activities, games, stories, history, admin
  shared/             what every feature uses and nothing domain-specific
    ui/               the primitives every screen is built from, each with its CSS next to it
    hooks/            browser APIs and screen patterns: back, online, wake lock, countdown,
                      theme and its 19:00/07:00 clock, requests, saves
    store.js          the local store
    http.js           `request` and the errors every call can throw
    format.js         failures and times in words
    theme.js          the night-mode rule
  styles/             tokens.css and base.css, imported by index.css
```

## Import rules

Nothing enforces these; they are checked in review.

- **`app → features → shared`, never upward.** `shared` imports nothing from `features` or `app`, and a feature imports nothing from `app`, with one exception: `home` takes the menu from `app/AppMenu.jsx`.
- **A feature imports another feature only through that feature's `index.js`,** which exports its screens, its data functions, and its types. Nothing else in a feature is imported from outside it. Types cross the same way (the toy form takes `Kid` from `family`).
- **Routes import only from `app` and feature indexes.** A route file declares its URL and names a screen.
- **`home` and `history` are the features that compose others.** Home: who's playing from `family`, the juego from `activities`, and the way into stories from `stories`. History: the juegos it opens through `activities`, and the stories through `stories`.

## Routes

The router plugin turns `src/routes/` into the route tree. A dot nests the path (`familia.revisar.jsx` is `/familia/revisar`), `$id` is a parameter, `index` is a path's own screen, and `__root.jsx` wraps every screen. Each screen is its own chunk, preloaded on intent.

| Route | Screen |
|---|---|
| `/entrada` | Entrada, with *Continuar con Google* first. `?error=` is the code a failed Google sign-in came back with (JUG-63) |
| `/cuenta` | Crear cuenta. `?modo=entrar` switches it to sign-in, `?campo=email` focuses a field, `?email=` fills the email, and `?error=` is as on Entrada |
| `/invitacion` | The link in an invitation email, with `?token=` and `?email=` (JUG-34). It checks the link first: an email with an account goes to `/cuenta?modo=entrar`, and an expired or replaced link says so. Otherwise *Te invitamos a Ludi*: a name and a password under the invited email, which can't be changed, or Google. `?error=` is as on Entrada |
| `/verificar` | Verificar el email. Redirects until the API sends email |
| `/bienvenida` | Bienvenida, the onboarding's first screen, right after an account is created with email or Google: the wordmark writes itself, and *Empezar* goes on to the family. An account with a family is sent Home (JUG-173) |
| `/familia/contanos` | Contame de tu familia, with the voice note on its mic |
| `/familia/revisar` | ¿Está bien así?, or Entendió mal when the parse flags fields |
| `/familia/corregir` | The correction form, the fallback path, and where a voice note says what changed (JUG-103): the parents, the kids, the pet and its animal, the home (JUG-21), and where they live (JUG-25). The toys are on it only in onboarding. `?campo=` focuses one field |
| `/familia` | Mi familia, the card's permanent home after onboarding. No toys: they live in the baúl (JUG-21) |
| `/juguetes` | El baúl de juguetes: the toys by family name. A voice note on its mic comes back as toys to confirm (JUG-146). Built from the primitives, with no design of its own |
| `/juguetes/$id` | One toy: its name, other names, what it is, whose it is, favorite, the toys it goes with, and *Ya no lo tenemos*. `/juguetes/nuevo` adds one, and a voice note fills its name and what it is (JUG-146) |
| `/materiales` | Materiales: the household materials by category, each a chip that saves on its tap (JUG-153). Built from the primitives, with no design of its own |
| `/ajustes` | Ajustes, not designed yet and kept minimal. **Cerrar sesión** lives here: it is account, not navigation. *Sobre Ludi* holds the license's legal notices: the copyright line, AGPL-3.0, no warranty, and the link to the source, which the license's added terms require. Don't remove it (JUG-171) |
| `/` | Home. The drawer, thinking, and offline are states of it. In the top corner, the weather the juego is picked for, and its line on a tap (JUG-191). With more than one kid, who's playing sits above the buttons (JUG-107). While a juego is played, its card takes the last juego's place with the time left and has *Terminamos* under it (JUG-134). Inside the last juego's card, under its words, the feedback tap, while the juego has no reaction (JUG-23). Under that card, *Con lo mismo*: the juego to play next with the materials this one needed (JUG-196). Above the buttons, *¿Algo en especial?* opens the sheet where the parent chooses what the juego should be, and once something is chosen it says what (JUG-31); from 19:00 it comes with *Tranqui* chosen, under *Antes de dormir* (JUG-26). Under it, the last story opened, with *Hacer una serie*, or *Leer otro episodio* for an episode (JUG-154), and *Otro cuento* beside it, which opens ¿Cuál leemos hoy? |
| `/idea/$id` | Actividad. Otro juego swaps in place and pushes history, so back returns to the previous one. A juego reached that way says *Juego anterior* on its back button and has a home button in the top bar (JUG-155). Under the juego, the feedback tap (JUG-23). Above the buttons, a line when no juego matched everything the parent chose and this one only came closest (JUG-31) |
| `/idea/$id/reloj` | El reloj. Under the hint, *Con lo mismo*, the juego to play next with the same materials (JUG-196) |
| `/idea/$id/que-suena` | ¿Qué suena?, the game a discovery juego's *Empezar* opens instead of the reloj (JUG-177): five rounds of a sound and its options under *¿Es…*, each option a tap that shows the answer once the sound has played, then *¡Listo!* with what they heard and the feedback tap |
| `/cuentos` | ¿Cuál leemos hoy?: two plots, the classic and a new one, and under them *Contame qué cuento querés escuchar*, which opens the mic hands-free beside a mic to hold (JUG-157). What the note asked for takes over the screen as *¿Este cuento?* until the parent writes it. The three stay above the fold on an iPhone SE in Safari, so the cards are kept tight and the waiting animation holds the two cards' height until the first plot lands (JUG-132). Under them, the family's series under *Series de cuentos*, each with *Leer otro episodio* (JUG-59), a chip per interest of the kids playing, without its article (JUG-140, JUG-190), and the two stories read last, each with *Hacer una serie* (JUG-50), with *Ver todo en Lo que jugamos* under them (JUG-189) |
| `/cuento/$id` | Escribiendo, with the waiting animation in the footer until the story's first paragraph (JUG-132), then the reading screen. Under the last paragraph, *Listo* goes Home, with *Hacer una serie*, or *Leer otro episodio* for an episode (JUG-154) |
| `/cuento/tema/$keyword` | The same reading screen, writing a story about an interest the parent tapped. Once saved, the URL is replaced with the story's own |
| `/cuento/pedido` | The same reading screen, writing the story the parent asked for by voice and said yes to, kept in the store as `storyRequest` until it is saved. Once saved, the URL is replaced with the story's own |
| `/serie/$id` | One series: its storyline, its episodes in order, *Leer otro episodio*, and *Ya no la seguimos* (JUG-59) |
| `/serie/$id/episodio` | The same reading screen, writing the series' next episode. Once saved, the URL is replaced with the story's own |
| `/historial` | Lo que jugamos (JUG-188): the juegos played and the stories read in the last month, under a heading per day, each opening again with one tap. Reached from the menu. Built from the primitives, with no design of its own |
| `/admin` | The catalog admin: every activity template, with its rating now and every family's reactions. Beside each, its *Puntaje inicial*, which saves as soon as it changes, and a switch that takes it out of *¡Juguemos!* On a phone both go under the words |
| `/admin/$id` | Edits one template, including its *Puntaje inicial*, 1 to 5, which every family's ranking starts from (JUG-192). `/admin/nuevo` adds one |
| `/admin/usuarios` | Usuarios: invite an email, with *Reenviar* for the invitations still open, and the accounts, each with *Borrar* (JUG-34, JUG-175) |

The admin is Alex's tool, not a parent's screen. It has no login yet (JUG-109), so the guard lets it through without an account and the API serves it only when `ADMIN_ENABLED` is true. That includes inviting people: no outside testers until the guardrails are complete. It is wider than the app (720 px) but still uses the tokens and the primitives.

## How it fits together

- **The session guard** (`app/guard.js`) runs before each navigation from `routes/__root.jsx`, and again from `app/RootLayout.jsx` when the session may have ended. It awaits `ensureSession()` from `features/account/api.js`, which confirms the session with `/api/me` at most every five minutes and when the app returns to the foreground.
  - **A 401 signs the device out,** from that check or any other call: `request` in `shared/http.js` (and the story stream) calls `endSession()`, except under `/auth`, where a 401 means a wrong password. No answer at all — offline, weak signal, a server failure — keeps the cached account, so the last juego stays readable.
  - A device whose session ended goes to `/cuenta?modo=entrar`; one that never had an account, or signed out, goes to `/entrada`. `signOut()` waits for the API, since only the server can end the httpOnly cookie; offline it says so and the parent stays signed in.
  - **First run holds its order,** in `firstRunTarget()`: no account goes to `/entrada`, and no family to `/familia/contanos`. When `/api/me` says `familyFromText` is false (no LLM), no family goes to the form at `/familia/corregir` instead. A new account lands on `/bienvenida` first (JUG-173): only an account without a family can open it, and its button goes to `/`, so the guard sends it on to the family. Signing in never shows it.
- **Sign in with Google** (JUG-63) is `GoogleButton` on Entrada and under the form on `/cuenta`, in both modes, through `useGoogleSignIn()`. Signing in and creating an account are the same tap. It asks the API for Google's address and leaves the app; Google comes back through the API, which starts the session and opens `/`, or `/bienvenida` for an account it has just created (`newUserCallbackURL`), where the guard takes over as on any first load. A failure comes back to the screen it started on with `?error=`, and `googleFailure()` words it; a parent who cancels at Google sees no message. Without a Google client on the server, the tap says Google isn't available.
  - **An invitation rides along** (JUG-34). On `/invitacion`, `signInWithGoogle` also sends the link's token as `additionalData.invitation`, which the API needs to let the email sign up, and the email as Google's `loginHint`. The email sign-up there sends the token as `invitation`. A Google account with another email comes back with `?error=INVITATION_OTHER_EMAIL`.
  - **The service worker leaves `/api/` navigations to the network** (`navigateFallbackDenylist` in `vite.config.js`). Without it, Google's return to `/api/auth/callback/google` would get the app instead of the API, and sign-in would never finish.
- **Build screens from the primitives** in `shared/ui/`, exported by its `index.js`, rather than one-off layouts: `Screen` (with `Header`, `Body`, `Footer`, `BackButton`), the buttons, `Card`, `Field`, `Chips`, `StepList`, `Drawer`, `Sheet`, `Dots`, `Waiting`, `DrawnWordmark`, `FinMark`, `PetalFall`, and `WeatherMark` (the one-off moments), `Skeleton`, `StatusLine`, `OfflineNotice`, `MetaLabel`, `Label`, `Wordmark`, `ThemeToggle`, and the icons in `Icons.jsx`. `Screen`'s `tone` sets the page background.
- **Screen patterns are hooks in `shared/hooks/`,** so no screen writes them again: `useRequest()` (`idle`, `loading`, `slow` after a delay, or `error` with the failure in words), `useOfflineNotice()` with `<OfflineNotice>`, `useSerialSaves()` for taps that save one after another, `useSlowWait()` for a wait that has gone on long enough to say something about, `useReducedMotion()`, and `useDocumentTitle()`.
- **Data comes only from each feature's `api.js`,** through `request` in `shared/http.js`. Screens never call `fetch` themselves and the web hardcodes no data. The two story streams and the voice upload call `fetch` directly, since `request` only speaks JSON, but they fail the same way. Screens work with the shapes in their feature's `types.js`, and `api.js` translates the API's shapes to them. `features/account/mock.js` holds only email verification.
- **Failures** go through `failureText()` in `shared/format.js`: the generic line, the offline line, or the words of a `WordedError` from `shared/http.js`. No error codes reach the screen.
- **Local state** is `shared/store.js`: one localStorage key per piece of state, listed in its `Key` typedef, read with `useStored(key)` and written with `write(key, value)`. It caches the account, the family, the toy box, the materials, activities, stories, the timer, story positions, and the history, which is what keeps the last juego and the open story readable offline. The typedef groups keys by the feature whose `api.js` writes them; screens read any key but write through their feature's `api.js`.
- **A long wait animates** (JUG-132, JUG-133, JUG-160). `Waiting` is the waiting language for the waits that take long enough that the dots alone look frozen, drawn from the tokens. Two variants: `ronda`, the wordmark's dots holding hands, for every wait but a story's; and `drawing`, for a story being written. The drawing's `tool` follows the age the story is written for, through `waitingTool()` over `playingAgeMonths()` — a crayon under three, a pencil to five, a pen after that — and its `mark` is the story's interest mark from `interestMark()`, or the tool's own scribble when there is none. It shows the dots for the first moment, so a wait that ends at once never animates, and it shows the dots for good when the phone asks for less motion. It is `aria-hidden`, so the words beside it carry the meaning. `busyMark` on a button puts it in the dots' place there, and a place that needs it smaller sets `--waiting-size`. A wait that passes `SLOW_AFTER_MS` also says in one plain line what is happening, the way Home does under *¡Juguemos!*.
- **Voice notes** (JUG-95) are two parts. `features/voice/recorder.js` holds the microphone and `MediaRecorder` and knows nothing of React, so a native client can replace it. `VoiceNote` is the mic: hold to record, slide up to lock, slide left or lift on *cancelar* to drop, lift anywhere else to send. Its `children` can be a function given `record`, for a button beside the mic that starts a hands-free note on a tap (JUG-157). No sound, no confirm step, no playback. `haptics.js` gives a short vibration when a note starts, locks, is sent, or is dropped (JUG-135). The audio lives only in the component, only for a retry, and is never stored.
  - **A note the screen has to read** uses `VoiceUnderstanding` (JUG-103, JUG-146): it wraps `VoiceNote`, hands the words to the screen's `read`, and shows "Leyendo" beside the mic while that runs. The screen puts the answer where the parent confirms it, and nothing is saved before that: the toy candidates in the baúl, the form in Nuevo juguete, the form in Editar, and the story request on ¿Cuál leemos hoy? (JUG-156). `VoiceLine` is the one line a note shows when it couldn't do its part, with "Mandar de nuevo" when the same words are worth another try; a `WordedError` from `read`, like "no escuché ningún juguete", is shown as it is and without the retry.
- **Stories open before they are asked for** (JUG-140). Home requests the options in the background when it mounts online and none are stored, so *Hora del cuento* opens with them on screen. `storyOptions()` keeps the request it has out and hands the same promise to a second caller. The options are forgotten when who's playing changes. Both story streams share `openStream` and `readEvents`, and each option is written into the store as it arrives, so the cards fill in while the model is still writing.
- **A story is written for the youngest kid playing,** which is the rule the API anchors on. `playingAgeMonths()` gives that age, and the story screens use it to pick the waiting animation, so the wait is for the same kid the story is (JUG-132).
- **Series are the family's own** (JUG-59). `series` in the store holds the list the API last sent, so ¿Cuál leemos hoy? and the series screen show it offline; the API's answer replaces it. An episode is a story like any other, cached under its own id, and `Story.series` says which series and episode it is. The episodes of a series are not on the library shelf: they are read under their series, and a series the family stops following gives its stories back to the shelf.
- **Interest marks** (JUG-160, JUG-166). `interestMark(...texts)` in `features/family/model.js` gives the `MarkIcon` mark for the first text that names a common interest, or null. Interest chips show it through `Chips`' `lead`, and `StoryMark` puts it in the corner of a story card (`story-marked`) from the story's title and teaser, or a series' storyline, and over the title on the reading screen (`place="title"`). `toyMark()` in the toys feature gives a toy's from its names and what it is, for the corner of its card in the baúl and the header of its screen. `characterMark()` gives the mark of whoever makes a sound in a story's legend: the pet by its animal, anyone else by their words. Never put a mark inside story text. A new mark follows the drawing rules in `docs/design.md`.
- **Sounds to act out** (JUG-170). A story's `sounds` are its legend, each `{ sound, who, how }`, and its text marks each sound between brackets. `StorySounds` shows the legend under the title, and `StoryText` sets each marked sound in bold through `soundPieces()`, so no bracket reaches the screen. A story being written gets its legend from the stream's `sounds` event, just before its first paragraph, so the legend never moves the text once reading starts. A story cached before this has no `sounds`, and neither does a template story.
- **Interests belong to each kid** (JUG-144). `Kid.interests` are that kid's own; the family has no list. `playingInterests()` gives what the kids playing love, for the story keywords. A family cached by an older version has its family-wide interests copied onto every kid on the next load.
- **The feedback tap is two chips** (JUG-23). `ReactionRow` shows *¡Lo hicimos!* and *No era para nosotros* for a juego, each led by its thumb and both on one line down to a 360 px phone, one pressed at most; tapping the pressed one takes the reaction back. `reactToActivity` writes the store first and puts it back if the API refuses, so the chip answers the tap. It sits under the juego on `/idea/$id`, where a reaction can always be changed, and on Home inside the last juego's card only while that juego has no reaction, staying through the tap so it doesn't vanish under the finger. Never a pop-up, never a sound, no counts.
- **Materials come from the API's list** (JUG-153). The categories, the materials, and which start marked are the API's; the web shows what `GET /family/materials` sends and saves one material per tap. A juego never needs a material the family has marked off, so the admin picks a template's materials from the same list.
- **Con lo mismo** (JUG-196). `SameMaterials` is one card on both the reloj and Home, under the juego whose materials are out. `chainActivity(id)` asks the API for a juego playable with them and keeps it in the store as `chain`, `{ from, activityId }`, so both screens show the same one and coming back doesn't ask for another; `activityId` is null when the catalog has nothing that continues it, which is remembered, so the card stops asking. Nothing shows while it is coming, for a juego that needs no materials, or when there is none: it is an offer, never a line saying there is none. A tap opens it like any other juego and leaves a running clock alone — its own *Empezar* takes the timer over. `ask` in `api.js` is the one call every suggestion goes through, and only `suggestActivity` writes `lastActivityId`, so a chained juego becomes the last one by being opened.
- **Toys have an id in the family.** `Family.toys` is `{ id, name }`, and onboarding's form sends each toy's id back: the API keeps what the toy box knows only about toys it recognizes, so a toy sent without its id comes back as a new toy with only a name. Mi familia's form has no toys and sends none, which leaves them as they are (JUG-21).
- **Where the family lives** (JUG-25) is one field on the family form, like the home: a city or a zone, in the parent's own words. It is read for one thing — whether the juego offered is one to play outside or one to play inside — and that happens on the server. Unlike the family's names, a place is public, so the field lets the keyboard correct and capitalise it, and it never asks the phone where it is. The API answers with the words and whether it found them, and `LocationField` says so when it didn't, because without the weather in Home's corner there is no other way for a parent to tell. Onboarding's card has no such field, so confirming it sends none and leaves whatever the family has.
- **The weather in Home's corner** (JUG-191) is `WeatherNote` in the activities feature. It shows what the ranking reads, from `GET /activities/weather`, never a temperature: `WeatherMark` draws it (a sun, heat, a cloud, fog, rain, a storm, wind, a wool hat for the cold, or the moon at night), once, and a tap plays it again and shows its line under it. `outsideLook()` in `activities/model.js` holds the drawings and the lines, and each line says what the ranking does: fine weather favours outside, poor weather and the night keep the juego in, and fair weather leaves it to the family. The API decides night, since the ranking follows it. `loadOutside()` asks on every visit to Home and on return to the foreground, and keeps the answer as `outside` for half an hour, so Home shows it at once; an older one is dropped first, so offline Home shows no weather rather than a stale one. Nothing shows while there is no weather.
- **Parents, the pet's animal, and the home** (JUG-21). The keys and their labels are `PET_KINDS` and `HOMES` in `features/family/model.js`, and must match `api/src/families/kinds.js`. `PET_KINDS` also carries the emoji the pet picker shows in place of the word, which is the web's own and not the API's. A parent card left without a name isn't saved, and one whose *Le dicen* was cleared is "Mamá".
- **Styles use the tokens** in `styles/tokens.css`. **Never write a hex value in a component or screen,** so nothing depends on a light background. A primitive's CSS sits next to it in `shared/ui/`, a feature's in `features/<name>/<name>.css`.
  - **The order is fixed:** `app/main.jsx` imports the tokens and base, then `shared/ui`, so the primitives' CSS always loads before a feature's and a feature can restyle a primitive at the same specificity (`.home__actions` over `.screen__footer`). In a production build, a feature the shell imports has its CSS in the main stylesheet; any other arrives with its screen's chunk. Check that an override still wins in a production build.
- **The parent chooses what the juego is** (JUG-31). `ChoiceChip` sits above *¡Juguemos!* and opens `ChoiceSheet`, *¿Qué juego buscan?*, on the `Sheet` primitive: four questions from `QUESTIONS` in `activities/model.js`, with one answer each at most. A tap saves at once and a second tap on the same answer takes it back; *Cualquier juego* and the chip's cross take everything back. Once something is chosen the chip is filled and says what, and a list too long for it ends in an ellipsis.
  - **The choices hold until the next switch,** like the theme's tap, in `activityChoices`. Without them `choicesNow()` gives the clock's: *Tranqui* from 19:00, under *Antes de dormir*, so an energetic juego doesn't come up at bedtime (JUG-26), and nothing the rest of the day. `useSwitchClock()` brings that in at 19:00 without a reload.
  - **`suggestActivity()` sends them,** so *Otro juego* carries the same choices. Where, sound, and kind are firm on the API; Tranqui and Con pilas are the moment, which leans the pick. When no juego matches every firm choice, the API sends the closest with `closest`, which travels to `/idea/$id` in the navigation's state, never in the store, so the line it shows there doesn't come back when the juego is opened again.
  - **The kinds are the catalog's categories** (JUG-30) but two: low energy and out and about are what *Tranqui* and *Afuera* already ask for.
- **Night mode** is dark from 19:00 to 07:00 unless a one-tap choice still holds until the next switch. The rule is plain functions in `shared/theme.js`; `ThemeProvider` re-checks it at each switch and on returning to the foreground, then paints `<html>` and the status bar; `applyInitialTheme()` sets it once before React renders. Components read `useTheme()`. `?tema=oscuro` or `?tema=claro` counts as a tap.
- **Lo que jugamos** (JUG-188) is `features/history/`: what `GET /history` sends, kept in the store as `history` so it opens offline. `historyDays()` puts the juegos and stories together under the phone's own days — *Hoy*, *Ayer*, the weekday, then the date — and never says how many days ago. A juego is there once *Empezar* was tapped on it, which `markPlayed()` tells the API without waiting, or once it got *¡Lo hicimos!*. A story is there once it was read, and `StoryReader` calls `markRead()` each time a story opens whole, so reading it again moves it up. A juego the device never kept is fetched with `findActivity()` before `/idea/$id` opens; a story opens at `storyKey()`, the key the device keeps it under, so one picked from the options opens offline too.
- **Discovery games** (JUG-128, JUG-177) live in `features/games/`. A juego whose `game` is set came with its game dealt by the API, and *Empezar* opens it instead of the timer; there is no other way in yet. `player.js` holds the phone's part and knows nothing of React: it fetches the game's five recordings into memory when the game opens, plays them from one `<audio>` element, only from a tap, and says the answer with the phone's own Spanish voice (`speechSynthesis`) until recorded answers exist. While the game is open the screen stays on, and on Safari 17 and later the sound plays with the ringer off. A round's `credit` is shown under its options when the recording's license asks for one.
- **Updates apply themselves** (`app/updates.js`). The page reloads at the next safe moment: never while a story is read or the timer runs, but once the parent leaves that screen or the app goes to the background. **A new screen a reload would interrupt goes in `BUSY` there.**
- **No inline scripts in `index.html`.** App logic goes in `src`, where it is built, cached, and reviewed with the rest.

## Adding a screen

1. Write it as `features/<feature>/screens/<Name>Screen.jsx` and export it from the feature's `index.js`. Read params and search with `useParams({ from })` and `useSearch({ from })`, since the screen doesn't import its route.
2. Add `src/routes/<path>.jsx` with only the declaration: `export const Route = createFileRoute('<path>')({ component: NameScreen })`, plus `validateSearch` when the URL has search params. No state, effects, JSX, or mapping.
3. Build it from the primitives and the hooks. Put its CSS in the feature's CSS file, tokens only. A screen over about 150 lines splits its parts into the feature's `components/`.
4. Set the title with `useDocumentTitle()`.
5. Get data from the feature's `api.js`, and cache in the store whatever must survive offline.
6. If it must be reachable before the family exists, update `firstRunTarget()` in `app/guard.js`.
7. Add it to the routes table above.

## Adding a feature

Every feature has the same layout, so there is one place for each file. A file a feature doesn't need is left out.

```
features/<name>/
  index.js        the public surface: screens, data functions, and types; nothing else is
                  imported from outside
  api.js          the endpoints, through `request`, and the store writes for this feature's keys
  model.js        pure logic with no React: form ↔ shape mapping, checks, text lines
  types.js        the shapes this feature works with, in JSDoc
  screens/        one component per route
  components/     pieces used only by this feature
  hooks/          when needed
  <name>.css      this feature's styles, tokens only, imported by its screens
```

Add the feature's store keys to the `Key` typedef in `shared/store.js`, under the feature's name.

## Calling a new endpoint

Write the function in its feature's `api.js`, calling `request` from `shared/http.js`. Translate the API's shape to the one in the feature's `types.js`, and export it from `index.js` only when another feature needs it. `request` throws `OfflineError` when the network fails and `ApiError`, with the status and code, when the API refuses, so screens and `failureText()` keep working unchanged. The endpoint's side is in [`api/AGENTS.md`](../api/AGENTS.md).

## Design rules that live in the code

[`docs/design.md`](../docs/design.md) has the whole set. These are the ones a code change undoes by accident:

- **Tokens only, never a hex.** The palette is the token set and nothing more. The one place hex is written out is `public/icon.svg` and `public/favicon.svg`, which are outside the app and can't read `tokens.css`; they change with the tokens.
- **Icons come from `shared/ui/Icons.jsx`.** Never write an `<svg>` in a screen or a feature: add the icon to the set, on the 24 px grid with a 2 px `currentColor` stroke, so it stays one weight and night mode needs no second drawing. `size` is the only prop worth changing. An icon is decorative (`aria-hidden`) and the button around it carries the label; an icon never replaces the words on a button.
- **No sound, ever,** including when the timer ends. The only vibration is the voice note's ticks (JUG-135). The one place sound plays is ¿Qué suena? (JUG-177), and only from the parent's taps there.
- **Motion plays but never holds anyone up** (JUG-159). Use the motion tokens (`--motion-quick`, `--motion-moment`, `--motion-stagger`, `--ease-out`, `--ease-play`) and the shared keyframes in `base.css` (`rise-in`, `pop-in`, `slide-in`) rather than new values. Nothing waits for an animation, nothing loops except a wait, and nothing celebrates an achievement. Give an arriving animation `backwards` fill and keep its element visible at rest, so reduced motion (which `base.css` turns off everywhere) shows it in its final state. The moments are listed in `docs/design.md`.
- **The family's words stay exactly as typed,** so those inputs set `autoCorrect="off"` and `autoCapitalize="none"`.
- **Colour is never the only signal.** Flagged rows get a tint, a bar, and words.
- **Nothing that counts or compares:** no streaks, points, badges, percentages, or days since the family last played. The only progress bar marks position in a story.
- **Thumb zone:** primary actions in the lower half, tap targets at least 48 px, design width 390 px capped with `max-width`.
- **Copy is Rioplatense Spanish with *vos*.** What Ludi suggests is a *juego*, never an *idea*, and Home's button says "¡Juguemos!" (JUG-69). Copy still waiting on a voice pass is marked `Voice pass pending` in the code.
